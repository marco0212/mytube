import React from "react";
import {
  SectionBox,
  SectionHeader,
  SectionTitle
} from "../style/styledComponents";

export default function PlayList() {
  return (
    <SectionBox start="4" end="5">
      <SectionHeader>
        <SectionTitle>Play List</SectionTitle>
      </SectionHeader>
    </SectionBox>
  );
}
